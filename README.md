# Java 11 Features – Complete Demonstration Project

This repository contains a comprehensive collection of examples demonstrating all major **Java 11 features**.  
It is designed as a learning resource and reference for developers who want to understand and experiment with the capabilities introduced in **Java 11**, the second LTS release after Java 8.

---

## 🚀 Features Implemented

This project includes hands-on code examples for all important Java 11 enhancements, such as:

### ✔ Core Language Enhancements
- Local variable syntax for lambda parameters (`var` in lambda)
- Improved type inference (var Allowed in Lambda Parameters)
- New string utility methods  
  - `isBlank()`  
  - `lines()`  
  - `repeat()`  
  - `strip() / stripLeading() / stripTrailing()`

### ✔ File and I/O Enhancements
- New methods added to `Files`  
  - `readString()`  
  - `writeString()`  
  - `copy()` updates

### ✔ HTTP Client (Standardized)
- Finalized `java.net.http.HttpClient`
- GET, POST examples
- Async & synchronous requests
- WebSocket support

### ✔ Collection Enhancements
- `Collectors` improvements
- `Optional.isEmpty()`

### ✔ Process API Enhancements
- Process handle updates

### ✔ Security and Cryptography
- TLS 1.3 support demonstration

### ✔ Deprecations & Removals
- Removed Java EE and CORBA modules
- Deprecation examples included

---

## 🛠 Technologies Used

- **Java 11**
- **Maven / Gradle** (include whichever you used)
- **VS Code / IntelliJ / Eclipse** (optional, add yours)
- **JUnit** (if tests exist)

